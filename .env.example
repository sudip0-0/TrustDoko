# App
NODE_ENV=development

# Database — Docker (port 5433 avoids conflict with local PostgreSQL on 5432)
DATABASE_URL="postgresql://trustdoko:trustdoko@127.0.0.1:5433/trustdoko"

# Auth.js — generate secret: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"

# File storage (optional until TD-0801)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
