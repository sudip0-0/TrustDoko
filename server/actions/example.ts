"use server";

/**
 * Server actions live in server/actions/.
 * Add domain actions here as features are implemented.
 */
export async function placeholderAction(): Promise<{ ok: true }> {
  return { ok: true };
}
